<Window x:Class="IkoboostWpf.Views.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

  <Border Background="{DynamicResource SurfaceBrush}"
          BorderBrush="{DynamicResource BorderBrush}"
          BorderThickness="1"
          CornerRadius="10">
    <Border.Effect>
      <DropShadowEffect BlurRadius="28" ShadowDepth="0" Opacity="0.5" Color="#000000"/>
    </Border.Effect>

    <StackPanel>

      <!-- ── En-tête (zone de déplacement) ───────────────────────── -->
      <Border Background="{DynamicResource SurfaceAltBrush}"
              CornerRadius="10,10,0,0"
              Padding="20,14"
              MouseLeftButtonDown="Header_MouseLeftButtonDown">
        <TextBlock x:Name="TitleText"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   FontSize="14" FontWeight="SemiBold"/>
      </Border>

      <!-- ── Contenu ────────────────────────────────────────────── -->
      <StackPanel Margin="24,20,24,0">

        <!-- Icône + message -->
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="52"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <Border x:Name="IconCircle"
                  Width="40" Height="40"
                  CornerRadius="20"
                  VerticalAlignment="Top">
            <TextBlock x:Name="IconText"
                       FontSize="16"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontWeight="Bold"/>
          </Border>

          <TextBlock x:Name="MessageText"
                     Grid.Column="1"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     FontSize="13"
                     TextWrapping="Wrap"
                     VerticalAlignment="Center"
                     LineHeight="20"/>
        </Grid>

        <!-- Liste d'éléments (optionnelle, ScrollViewer si longue) -->
        <Border x:Name="ItemsBorder"
                Visibility="Collapsed"
                Background="{DynamicResource SurfaceAltBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Margin="0,14,0,0">
          <ScrollViewer MaxHeight="180"
                        VerticalScrollBarVisibility="Auto"
                        Padding="12,8">
            <ItemsControl x:Name="ItemsList">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding}"
                             Foreground="{DynamicResource TextSecondaryBrush}"
                             FontSize="12"
                             Margin="0,2"/>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
        </Border>

        <!-- Option point de restauration (optionnelle) -->
        <CheckBox x:Name="RestoreCheckBox"
                  Foreground="{DynamicResource TextSecondaryBrush}"
                  FontSize="12"
                  Visibility="Collapsed"
                  Margin="0,14,0,0">
          <TextBlock TextWrapping="Wrap">
            <Run Text="🛡 Créer un point de restauration Windows avant de continuer"/>
          </TextBlock>
        </CheckBox>

      </StackPanel>

      <!-- ── Boutons ────────────────────────────────────────────── -->
      <Border BorderBrush="{DynamicResource BorderBrush}"
              BorderThickness="0,1,0,0"
              Padding="24,14"
              Margin="0,20,0,0">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button x:Name="CancelButton"
                  Content="Annuler"
                  Style="{StaticResource SecondaryButtonStyle}"
                  Click="Cancel_Click"
                  MinWidth="90"/>
          <Button x:Name="ConfirmButton"
                  Style="{StaticResource DangerButtonStyle}"
                  Click="Confirm_Click"
                  Margin="10,0,0,0"
                  MinWidth="90"/>
        </StackPanel>
      </Border>

    </StackPanel>
  </Border>
</Window>
